name: Manual Regression Trigger
on:
  issue_comment:
    types: [created]

jobs:
  prepare:
    runs-on: ubuntu-latest
    if: github.event.issue.pull_request != '' && contains(github.event.comment.body, '/regression')
    steps:
      - name: get pr details
        id: pr_details
        run: |
          PR_DATA=$(curl -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                       -H "Accept: application/vnd.github.v3+json" \
                       https://api.github.com/repos/${{ github.repository }}/pulls/${{ github.event.issue.number }})
          echo "branch=$(echo $PR_DATA | jq -r .head.ref)" >> $GITHUB_ENV
          echo "repo=$(echo $PR_DATA | jq -r .head.repo.full_name)" >> $GITHUB_ENV

      - name: Checkout pr branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          repository: ${{ env.repo }}
          ref: ${{ env.branch }}

      - name: Verify current branch
        run: |
              echo "Current branch: $(git rev-parse --abbrev-ref HEAD)"
              echo "Current commit: $(git rev-parse HEAD)"


  end-to-end-tests:
    needs: [prepare]
    uses: ./.github/workflows/e2e.yaml
    with:
      ref: ${{ env.branch }}
